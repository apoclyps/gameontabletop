# Copy to .env and fill in values

# Backend — used by docker-compose and local dev
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/app

# Frontend — leave empty in dev (Vite proxy handles it); set to backend URL in production
VITE_API_BASE_URL=
